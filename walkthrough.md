# 공지사항 관리자 전용 권한 구현 완료

공지사항 등록 기능을 관리자만 사용할 수 있도록 인증 구조를 강화하고 관련 UI를 수정했습니다.

## 주요 변경 사항

### 1. 관리자 권한(Role) 체계 도입
- 데이터베이스 `users` 테이블에 `role` 컬럼을 추가했습니다. (일반 유저는 'user', 관리자는 'admin')
- 기본 관리자 계정을 시스템에 주입해 두었습니다.
  - **아이디**: `admin`
  - **비밀번호**: `admin123`

### 2. 백엔드 보안 강화 (`routes/notices.js` & `authMiddleware.js`)
- 공지사항 등록 API(`POST /api/notices`)에 `authenticateAdmin`이라는 새 미들웨어를 적용했습니다.
- 로그인 시 발급되는 JWT 토큰을 해석하여 권한이 `admin`인지 검사하며, 권한이 없거나 토큰이 없으면 `403 Forbidden` 혹은 `401 Unauthorized` 에러를 반환해 백엔드 단에서 원천 차단합니다.

### 3. 프론트엔드 로그인 및 UI 반영 (`auth.html` & `notice-board.html`)
- **로그인 연동**: 이전에 만들어져 있던 프론트엔드 로그인 탭(`auth.html`)을 실제 백엔드 API와 연결했습니다. 로그인을 성공하면 서버로부터 발급받은 JWT 토큰과 유저 정보를 브라우저의 `localStorage`에 저장합니다.
- **글쓰기 버튼 숨김**: `notice-board.html`에서 현재 접속한 사용자의 정보를 확인합니다.
  - 관리자 계정(`admin`)으로 로그인한 상태일 때만 `+ 공지 작성` 버튼이 노출됩니다.
  - 비로그인 상태이거나 일반 계정으로 접속하면 작성 버튼이 완전히 사라집니다.

## 테스트 안내
1. 브라우저 주소창에 `/notice`로 바로 접속하시면 글쓰기 버튼이 보이지 않습니다.
2. 메인 페이지 우측 상단 메뉴를 통해 **로그인/회원가입**(`auth.html`) 페이지로 이동합니다.
3. 아이디 `admin` / 비밀번호 `admin123` 으로 로그인을 진행합니다.
4. 다시 공지사항(`/notice`) 메뉴로 돌아오시면 `+ 공지 작성` 버튼이 나타나는 것을 확인하실 수 있으며, 글 작성 역시 정상적으로 완료됩니다.
